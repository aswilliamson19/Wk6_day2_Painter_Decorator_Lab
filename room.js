const Room = function(area, isPainted) {
  this.area = area
  this.isPainted = isPainted
}

// Room.prototype.addPaint = function(paintedLevel) {
//   if paintedLevel < area
//   return 'Can be painted'
// }

module.exports = Room
